@model BookStoreMVC.Data.BookDbContext
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
</div>

@* 
<form method="post" enctype="multipart/form-data">
    <input type="file" name="file" multiple accept=".docx, .pdf, .txt "/>
    <button type="submit">Upload File</button>
</form>
@if(ViewBag.Message != null)
{
    <div class="alert" style="margin-top:26px">
        @ViewBag.Message
    </div>
}

<p style="margin-top: 50px">List of Files</p>
@if (Model?.Files != null)
{
    <!-- Get all the files from the server -->
    <ul>
        @foreach (var item in Model.Files)
        {
            <li>
                <a asp-action="Download"
                   asp-route-filename="@item.Name">
                    @item.Name
                </a>
            </li>
        }
    </ul>
}
else
{
    <p>No files available.</p>
} *@